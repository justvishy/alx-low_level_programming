#include "main.h"
#include <string.h>

/**
 * _strlen - this is a prototype function
 *
 * @s: this is a character
 * Return: the length of the string
 */

int _strlen(char *s)
{
	return(strlen(s));
}
