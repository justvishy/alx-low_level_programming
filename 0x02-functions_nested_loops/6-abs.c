#include "main.h"
#include <inttypes.h>

/**
 * _abs - this is a function
 * @n: this is the input
 * Return: The absolute value
 */

int _abs(int n)
{
	int j = n;
	intmax_t imaxabs(intmax_t j);

	if (j < 0)
	{
		return (-j);
	}
	else
	{
		return (j);
	}
}
